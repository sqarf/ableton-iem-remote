{
  "name": "ableton-iem-remote",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local phone control for member-specific Ableton in-ear monitor sends",
  "scripts": {
    "start": "node server/main.js",
    "test": "node --test test/*.test.js",
    "check": "node scripts/check-syntax.js"
  },
  "engines": {
    "node": ">=20"
  }
}
